package com.mirth.connect.client.ui;

import java.awt.Color;

/**
 * Compile-time stub of Mirth Connect's real
 * {@code com.mirth.connect.client.ui.UIConstants} (ships in the
 * Administrator's mirth-client.jar / core-ui module).
 *
 * <p>Only the constant this plugin uses is mirrored. The real value,
 * verified against the Mirth Connect source (UIConstants.java line 104),
 * is {@code public static final Color INVALID_COLOR = Color.PINK;} - Mirth
 * marks invalid fields by painting their BACKGROUND with this color
 * (see MLLPModeSettingsDialog.checkProperties / resetInvalidProperties).</p>
 *
 * <p>NEVER package this stub - the real class ships inside Mirth.</p>
 */
public class UIConstants {

    /** Mirth's own "invalid field" highlight color. */
    public static final Color INVALID_COLOR = Color.PINK;
}
