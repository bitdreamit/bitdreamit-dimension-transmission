package com.mirth.connect.client.ui.components;

import javax.swing.JComboBox;

/**
 * Compile-time stub of Mirth Connect's real
 * {@code com.mirth.connect.client.ui.components.MirthComboBox}
 * (Administrator's mirth-client.jar / core-ui module).
 *
 * <p>IMPORTANT (mirrors the real class exactly): MirthComboBox has ONLY a
 * no-argument constructor. To pre-fill items, use addItem(...) after
 * construction - never an items-array constructor, which does not exist
 * and would fail to compile / link against the real Mirth client jar.</p>
 *
 * <p>NEVER package this stub - the real class ships inside Mirth.</p>
 */
public class MirthComboBox<E> extends JComboBox<E> {

    public MirthComboBox() {
        super();
    }
}
